name: glow_up_app
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_card_swiper: ^7.0.1
  get: 4.6.6
  http: ^1.2.1
  intl: ^0.19.0
  string_extensions: ^0.7.1
  transitioned_indexed_stack: ^1.0.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/images/
